# Example Infrastructure Repository

This directory is the golden infrastructure-profile repository for configuration, shared tooling, deployment automation, and operational assets.

Detailed documentation starts at [docs/index.md](docs/index.md).

A real infrastructure repository must document bootstrap from an empty environment, dependencies, permissions, secrets, deployment topology, local validation, operations, backup, restore, rollback, and disaster recovery.
